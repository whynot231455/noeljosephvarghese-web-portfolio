# Project cover images

Each project can have its own folder here:

  public/projects/<project-id>/cover.png

Reference it from the project's JSON as:

  "imageUrl": "/projects/<project-id>/cover.png"

Or use a remote URL instead (`https://...`) — no upload needed.
See CONTENT_GUIDE.md in the repo root and src/content/projects/_template.json.
